import { ParcelaPagaDirective } from './parcela-paga.directive';

describe('ParcelaPagaDirective', () => {
  it('should create an instance', () => {
    const directive = new ParcelaPagaDirective();
    expect(directive).toBeTruthy();
  });
});
