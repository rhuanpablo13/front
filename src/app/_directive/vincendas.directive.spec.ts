import { VincendasDirective } from './vincendas.directive';

describe('VincendasDirective', () => {
  it('should create an instance', () => {
    const directive = new VincendasDirective();
    expect(directive).toBeTruthy();
  });
});
